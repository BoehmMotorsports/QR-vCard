# vCard QR Code Generator

A simple Python GUI application to generate vCard QR codes for business cards.

## How to use
1. Install dependencies: `pip install -r requirements.txt`
2. Run the app: `python gui_qr.py`
